.DS_Store
swing-trade-frontend-day2.zip
Files_Archives/

# Node dependencies (Day 82: untracked — 40,801 files / ~101MB accidentally
# committed, found by the Day 82 Fable process/hygiene audit)
frontend/node_modules/

# Environment variables (API keys - never commit)
.env
backend/.env

# Python virtual environment (Day 78: untracked — was accidentally committed)
backend/venv/

# Verdict parity grid test artifacts (Day 78) — large, fully regenerable via
# `python backend/backtest/test_verdict_parity.py`. Findings are documented
# in docs/claude/versioned/ instead of committing raw JSON dumps.
backend/backtest/verdict_grid_inputs.json
backend/backtest/verdict_grid_py_output.json
backend/backtest/verdict_parity_mismatches.json
frontend/scripts/verdict_grid_js_output.json

# Scratch/debug run artifacts (Day 82 hygiene pass — 21 stale files like this
# had accumulated untracked in backend/ root over many months). One-shot
# script output belongs in docs/claude/versioned/ as a written-up finding,
# not as raw dumps sitting in the repo root.
backend/*.log
backend/backend.log
backend/cache.db

# Frontend dev-server log (Day 84: start.sh now redirects `npm start`'s
# output here instead of the launching terminal, so the backend/frontend
# survive a closed terminal window without their stdout fd dying).
frontend/frontend.log
backend/scan_test_results_*.json
backend/test_results_*.json
backend/validation_report_*.json
backend/backtest_*results*.csv
backend/coherence_test_results_*.json

# Paper-trading ledger + its local rolling backups (Day 82) — the live SQLite
# ledger itself was already untracked; these are its safety-net copies
# (ledger.backup_db(), called after every daily_job.py run). Not meant for
# git — this is a local-disk safety net, not a version-controlled artifact.
backend/validation_results/paper_trading_ledger.db
backend/validation_results/ledger_backups/

# Runtime SQLite state (Day 82) — was already untracked de facto (nobody had
# git-added it), making it explicit now: the OHLCV/fundamentals cache and the
# new shared rate-limiter/circuit-breaker state (provider_state.db) are both
# regenerable local runtime data, not version-controlled artifacts.
backend/data/
