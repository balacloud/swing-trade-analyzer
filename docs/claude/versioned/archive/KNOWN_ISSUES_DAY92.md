# Known Issues — Day 92 (July 20, 2026)

## Changes from Day 91

**Resolved this session:**
- Paper-trading's `signal_date` was stamped from the wall clock instead of the OHLCV bar `signal_price` came from — a weekend/off-hours Force Run could permanently strand a signal in `pending_entry` with no visible error. 8 of momentum's 11 pending signals were affected; all repaired, 7 reactivated immediately. See Golden Rule 28.

**Added this session:**
- Volume-confirmation gap in the decision engine (new, see below) — found via a first-principles review, not a live bug report.
- MR's ADX/docstring mismatch (new, see below) — same review.

**Freeze status:** superseded — forward-testing accumulation is now the user's explicitly stated **sole** priority (stricter than the prior "bug fixes + monitoring" freeze). Confirmation bar raised 50 → 100 trades/system.

**Found and fixed in a follow-up monitoring session (2026-07-22, Sectors tab review — unrelated to paper trading, does not touch the freeze):**
- **Cap Size Rotation banner was mid-cap-blind.** Both the header signal (`backend.py`'s `size_signal`/`size_signal_detail`) and the frontend's momentum sub-note (`SectorRotationTab.jsx`'s `momentumNote()`) computed their text from QQQ (large) and IWM (small) only — MDY (mid) was fetched and displayed as a tile but never fed into either headline. Caught live: the sub-note read "⚠️ Risk appetite fading across all cap sizes" while MDY's own tile in the same screenshot showed positive momentum ("Improving · recovering") — a direct contradiction on real data, same class of bug as Day 91's false "100=market parity" claim on this same tab.
  - Fix: `momentumNote()` now considers all 3 tiers and only claims "all cap sizes" when literally all 3 agree; otherwise it names which tier(s) are up vs. down (e.g. "Mixed: mid cap gaining, large/small fading"). The header's Neutral-case label was reworded from "Mixed signals across cap sizes" to "Large vs. small caps — no clear tilt" since that specific signal is deliberately a 2-ETF (large vs small) comparison, not a 3-way one — the old wording overclaimed its own scope.
  - Verified live (claude-in-chrome): both corrected strings render correctly on the actual Sectors tab, zero console errors. Backend's `SECTOR_ROTATION` cache entry was invalidated to pick up the fix immediately rather than waiting for next-market-close expiry.
  - **Second pass (Golden Rule 13, exhaustive not spot-check):** the first live check only exercised the one branch that day's real market data happened to hit. Ran all 8 up/down permutations of QQQ/MDY/IWM programmatically — caught a real grammar bug the spot-check couldn't have (`"large/mid cap gaining"` used singular "cap" regardless of how many tiers were listed). Fixed to pluralize correctly (`"caps"` when >1 tier); re-ran the full 8-combo test against the fix, then re-verified live in-browser. Zero-exact-momentum edge case (all 3 at momentum=0.0) intentionally left classified as "down," matching the existing `tileInterpretation()` convention elsewhere in the same file — not a new inconsistency.
  - Display/wording-only — no verdict, scoring, or trading logic touched. Independent of the paper-trading freeze.
- **Cap Size Rotation tiles' bar/number color contradicted their own quadrant label.** User-caught: MDY's bar was red while its label said "Improving · recovering" (blue), and QQQ's bar was green while its label said "Weakening · fading" (yellow) — same tile, two color signals disagreeing. Root cause: the bar/number color (`SectorRotationTab.jsx`) was driven by raw RS Ratio (≥100 = green, <100 = red) while the label was driven by `tileInterpretation()`'s quadrant (RS Ratio *and* momentum together, 4-color scheme). A reader pattern-matching on green=good/red=bad got the opposite signal from the actual quadrant call.
  - Fix: `tileInterpretation()` now also returns `barColor`, matching its own `color` (green=Leading, blue=Improving, yellow=Weakening, red=Lagging) — bar and number both now track the same quadrant color as the label, eliminating the raw-RS-Ratio-only coloring entirely for this strip. The main 11-sector grid was unaffected (it already shows two separately-labeled bars — RS Ratio and RS Momentum — so no conflation existed there).
  - Verified: all 4 quadrant combinations + a 100/0 boundary case tested programmatically for self-consistency before touching the browser; re-confirmed live (MDY tile now blue bar+label, IWM now yellow bar+label).
- **Sectors tab redesigned for beginner interpretability (user-requested, same session).** User asked directly: is the ranking meaningful, why did a "Weakening" sector (Technology) drive the top CTA, and how would a quant trader design this for a beginner. Reworked `SectorRotationTab.jsx`:
  - `pickBestSector()` (new): the header CTA now picks the top Leading sector by RS Ratio (falling back to top Improving, then a disabled "No leading sector right now" state) instead of whichever sector has the highest RS Ratio regardless of quadrant — this is what actually fixes the Priority #10 "Rank #1 CTA bypasses the Leading/Improving gate" finding, not just a rename.
  - `buildTakeaway()` (new): a one-line plain-English summary ("2 sectors leading right now — strongest setups in Financials, Energy.") rendered above the grid, with honest fallback text when nothing is Leading/Improving.
  - Sector cards now render grouped by quadrant (Leading → Improving → Weakening → Lagging, each with a one-line hint of what to do with that group) instead of one flat list ordered by RS-Ratio-magnitude rank.
  - **Follow-up (same session, user-caught):** the per-card badge was first relabeled "RS Rank #N" rather than removed — user immediately pointed out that any "#1" reads as "the winner" regardless of what qualifier precedes it, so "RS Rank #1" next to a "Weakening" badge was still self-contradictory on its face. Removed the ordinal badge entirely (`rankBadgeClass()` deleted as dead code); the card header now shows only the quadrant badge. The RS Ratio *value* itself is unaffected — still shown in the card body via the existing labeled "RS Ratio" bar — only the "position among 11, framed as a rank" presentation was removed. Explanation row's text updated to match.
  - Cap Size Rotation strip gained a plain-English framing line ("Is money moving into risk (small caps) or safety (large caps)?").
  - Verified: `pickBestSector`/`buildTakeaway` tested programmatically against the real screenshot data plus 3 edge cases (no-Leading, broad-weakness/no-Leading-no-Improving, empty) before touching the browser; confirmed live — CTA now reads "Scan Top Sector: Financials (Leading)", all 11 sectors correctly distributed across the 4 quadrant groups (2/4/2/3), zero console errors.
  - No backend/computation changes — RS Ratio, RS Momentum, and quadrant classification are untouched. Purely a presentation reorganization of already-correct data.
- **Context tab audit + Sectors↔Context cross-tab connection (Day 92 continued, planned via a Plan-mode/Opus pass against `GOLDEN_RULES.md`, then scoped down and confirmed with the user before building).** User asked whether the market regime/fundamentals actually support the sector rotation being shown, then asked for the Context tab to get the same audit treatment as Sectors and for the two tabs to "connect the dots."
  1. **Real bug, confirmed a Day 91 regression: `econ_engine.py`'s composite narrative box silently ignored the PMI proxy's actual regime.** `_build_composite()` looked up `card_map.get('ISM PMI (proxy)', ...)`, but Day 91 had renamed the card to `'Manufacturing Employment (PMI proxy)'` (`KNOWN_ISSUES_DAY91.md` even flagged "re-check the composite reflects it" — it was never done). The lookup always missed, so `pmi` was permanently stuck at `'NEUTRAL'`, silently disabling 3 of 5 narrative branches (Rising Growth, Goldilocks, Contractionary PMI) regardless of the real indicator. A second variable, `unemp`, was computed but referenced in zero branch conditions — dead code, Unemployment never actually influenced the narrative despite the box claiming to synthesize all "4" indicators. Fixed via a shared `PMI_CARD_NAME` constant referenced by both `_pmi_card()` and `_build_composite()` (so a future rename can't cause the two to silently diverge again) and deleted the dead `unemp` line. Verified by exhaustively testing all 27 fed×cpi×pmi combinations (not a spot-check) — confirmed all 5 branches now reachable; today's live output is unchanged (PMI genuinely is NEUTRAL today), but the logic is now correct for any other day.
  2. **Structural fix: Seasonal Regime card's May–Oct text contradicted its own NEUTRAL (yellow) badge.** `_seasonal_regime()` can only ever return FAVORABLE or NEUTRAL (ADVERSE is unreachable for this card), yet its May–Oct text said `'"Sell in May" period'` / `'reduced exposure justified'` — cautionary, action-oriented language next to a yellow "everything's fine" badge, live on screen the day this was found (today is July, in-season). Same failure class as the Sectors tab's color-vs-label bugs. User confirmed the fix direction: soften the text to genuinely match NEUTRAL rather than give the card a real ADVERSE state (which would have changed the `overall_regime` aggregate's adverse count every May–Oct — a bigger, aggregate-altering change requiring separate sign-off). Verified across all 12 months programmatically; confirmed live — card now reads "Weaker seasonal window" / "a mild seasonal tilt, not a standalone risk-off signal," badge unchanged.
  3. **New cross-tab connection: Sectors tab now states whether the macro backdrop supports the sector rotation it's showing** — directly answering the question the user had to manually cross-reference two tabs (and two direct API calls) to answer earlier this session. Backend: extracted the `overall_regime` aggregation (previously inlined in `/api/context/<ticker>`) into a shared `compute_overall_regime()` helper; `get_sector_rotation()` now calls the same FRED-cached cycles/econ engines (no new Alpha Vantage or yfinance calls — deliberately not calling `/api/context/<ticker>`, which would burn AV news quota on a placeholder ticker) and derives one plain-English sentence + a `aligned`/`cross_current`/`neutral` status from {overall_regime} × {any sector Leading?}. Rides along on the existing cached `sectorRotation` response (`macro_alignment`, `macro_alignment_status` fields) — zero new frontend fetch, zero coupling between the two React components. Frontend renders it as one line under the Sectors tab's existing takeaway sentence.
     - **Second bug caught only by live verification, not code review:** the new fields didn't render at all on first attempt — traced to `api.js`'s `fetchSectorRotation()`, which doesn't pass through the raw backend JSON but manually reconstructs a whitelisted object literal, silently dropping any field not explicitly listed. Fixed by adding `macro_alignment`/`macro_alignment_status` to that whitelist. A pure code-read would not have caught this — the backend response was correct, the bug was entirely in an intermediate frontend transform. Golden Rule 6 (verify live, don't just claim it works) directly earned its keep here.
     - Verified: table-tested the alignment sentence across all 9 {FAVORABLE,NEUTRAL,ADVERSE}×{0,1,2+ Leading} combinations before touching the browser; confirmed live end-to-end (backend field → frontend whitelist → rendered banner), zero console errors. Today's real output: "Macro backdrop is NEUTRAL — neither confirming nor contradicting the current sector picture."
  - Freeze isolation independently verified (via the planning pass): none of `cycles_engine.py`, `econ_engine.py`, or `market_phase_engine.py` are imported by `categoricalAssessment.js`, `mean_reversion.py`, or anything under `backend/paper_trading/` — none of this touches the frozen verdict engine or the paper-trading gate.
- **Market Phase ⟷ Overall Macro Regime reconciliation (Day 92, same-session follow-up to the item above — no longer deferred).** The Context tab's own two top banners — Market Phase (price/technical: SPY trend + VIX) and Overall Macro Regime (10 FRED+calendar indicators) — were independent reads stacked with zero reconciliation; a user could see "Bull Rally" directly above "NEUTRAL" macro regime with no explanation of whether that's a contradiction (it isn't — they measure different things: price momentum vs. macro/fundamental support).
  - **Pure frontend fix, no backend changes** — `ContextTab.jsx` already held both `marketPhaseData.phase` and `overallRegime` in state. Added `reconcilePhaseRegime(phase, overallRegime)`: buckets the 5 phases into Risk-On (Bull Rally/Late Bull), Risk-Off (Distribution/Correction), or Recovery (handled as its own always-tentative case), crosses that against the 3 macro regimes, and renders one plain-English sentence + `aligned`/`cross_current`/`neutral` status between the two banners — same status vocabulary and color language as the Sectors↔Context `macro_alignment` connection, so the whole app now speaks one consistent language for "do these two signals agree."
  - **Exhaustive test caught a real defensive-coding gap before it shipped:** the initial version's fallback branch assumed anything that wasn't Risk-On or Risk-Off must be "Recovery" — an unrecognized phase string would have silently mislabeled itself as "attempting an early recovery." Not live-reachable today (the backend's `phase_grid` only ever returns one of 5 fixed strings), but cheap to harden: changed to an explicit `phase === 'Recovery'` check, falling back to `null` (hides the banner) for anything unexpected — same fail-silent-don't-block pattern the rest of the informational tabs already use.
  - Verified: all 15 phase×regime combinations tested programmatically before touching the browser (5 phases × 3 regimes), plus null/unknown-phase edge cases; confirmed live — today's actual reconciliation reads "Price is in Bull Rally; macro backdrop is NEUTRAL — momentum without a strong fundamental tailwind," zero console errors.
- **Critical pass against `GOLDEN_RULES.md` on this session's own work (Day 92, self-audit) — found 2 real, self-inflicted issues.**
  1. **Golden Rule 21 (DRY the shared logic) violation:** `SectorRotationTab.jsx`'s `MACRO_ALIGNMENT_STYLES`/`_ICONS` and `ContextTab.jsx`'s `RECONCILIATION_STYLES`/`_ICONS` were byte-identical maps, written independently a few edits apart rather than shared — the exact anti-pattern Rule 21 exists to prevent ("DRY the shared logic before writing the second implementation, not after finding a drift bug"). Extracted both into a new `frontend/src/utils/alignmentStyles.js` (matching the existing `liquidityThresholds.js` precedent for small shared-constant modules); both components now import from it. Verified live post-refactor: both tabs still render correctly, zero console errors.
  2. **Golden Rule 21's other explicit requirement — "verify an extraction is behavior-preserving before relying on it" — had not actually been done** for the `compute_overall_regime()` extraction earlier in this session. Closed the gap: re-hit `/api/context/SPY` post-extraction and confirmed the response (`overall_regime: NEUTRAL`, `regime_counts: {favorable:2, neutral:7, adverse:1}`, `total_indicators:10`) is byte-identical to the response captured earlier in this same conversation, before the refactor.
  3. **Documentation self-contradiction:** this same file's own "Open Issues" section (further down) still listed "Sectors tab's Rank #1 CTA bypasses the same Leading/Improving gate" as an unresolved Low/Info item — but that is exactly what `pickBestSector()` fixed earlier in this session. Fixed here and in `ROADMAP.md`'s matching Priority #10 line, which had the identical stale reference. Same failure mode as the CPI/PMI-composite bugs this session already found elsewhere: a real fix landing without the surrounding docs being swept for now-stale mentions of the same finding.

---

## Open Issues

### Medium: Backtest↔Live Fundamentals Data-Source Mismatch (carried from Day 78/79)
**Severity:** Medium
**Description:** 40.0% disagreement rate between live (Finnhub/AlphaVantage/yfinance TTM) and backtested (SimFin quarterly) Fundamental labels, measured on 20 liquid tickers. Revenue growth is the dominant driver. Also relevant to the automated paper-trading engine's momentum leg.
**Fix:** Mitigation choice still a pending user decision. Parked behind the paper-trading focus per Day 92.

### Medium: Canadian Market — Analyze Page Not Yet Supported (carried from Day 59)
**Severity:** Medium (incomplete feature)
**Description:** v4.21 Canadian support only works for Scan tab. Analyze page needs data source redesign for `.TO` tickers.
**Fix:** Parked behind the paper-trading focus per Day 92.

### Low / Info: Volume confirmation missing from the decision engine (new, Day 92)
**Severity:** Low
**Description:** Neither the Full Analysis verdict tree (`determineVerdict()`) nor the Simple Checklist's 9 criteria check whether a price move is confirmed by rising volume (accumulation) vs. thin volume. The Simple Checklist's "Volume" criterion is a liquidity gate (avg $ volume vs. cap-tier threshold), not a price/volume-confirmation signal — confirmed live against a real ticker (a screenshot showed CCO passing "Volume: PASS" on liquidity alone). OBV trend/divergence exists as a separate informational card and VCP/Cup&Handle's volume-dry-up check exists inside pattern detection, but neither feeds the verdict or the checklist pass count.
**Fix:** Deferred — adding it touches the frozen, already-backtested verdict logic and needs a fresh walk-forward/survivorship-free re-backtest before shipping. Tracked as ROADMAP.md Priority #11. Explicitly parked until the 100-trade paper-trading gate clears.

### Low / Info: MR's ADX docstring doesn't match its code (new, Day 92)
**Severity:** Low
**Description:** `mean_reversion.py`'s module docstring claims MR is "only active when ADX < 20 (range-bound)," but `detect_mr_signal()`'s actual `signal` condition never checks ADX — it's computed and returned as an informational `range_bound` flag only. May be a doc-accuracy issue rather than a bug (Connors' published RSI(2) method trades dips within an established uptrend, not specifically range-bound markets) — the PF 1.16 backtest result already reflects this exact ungated code, so nothing here invalidates prior results.
**Fix:** Deferred alongside the volume-confirmation item, ROADMAP.md Priority #11.

### Low / Info: Session 28 audit's remaining lower-priority findings (carried from Day 91)
**Severity:** Low
**Description:** Value tab's ROE thresholds badged "Buffett/Damodaran" when the code comment says "ChatGPT research validated"; Validate/Data Sources tabs show "live"/"healthy" status without probing real fetch success; Sectors tab's `.toFixed(3)` false precision (the Rank #1 CTA/gate-bypass part of this same finding was fixed later Day 92 by `pickBestSector()` — see above, not a duplicate open item); Forward Testing's momentum-path trades store identical net/gross P&L and per-position fetch failures are silently dropped. Plus the audit's general polish list.
**Fix:** Tracked as ROADMAP.md Priority #10 — batchable, not urgent, parked behind paper-trading focus.

### Low / Info items (carried forward, unchanged)
SimFin key rotation unconfirmed, Defeat Beta import present, Scan tab breakout badge NOT_READY vs failed-fetch ambiguity, Master Framework/Nirmal watchlist Name/Market Cap N/A by choice, README.md version footer drift (now partially addressed Day 92 — Completed list and Current Priorities refreshed, but the file's own header/badge versioning wasn't audited line-by-line). See `KNOWN_ISSUES_DAY87.md` for full text of the pre-existing items.
